# AI Vision Lab — runtime dependencies
# Pin conservative, mutually compatible ranges (verified with Python 3.11+).
# protobuf <5 is required: MediaPipe 0.10.x still calls
# MessageFactory.GetPrototype, which protobuf 5+ removed.

numpy>=1.26,<3
opencv-python-headless>=4.10,<5
mediapipe>=0.10.21,<0.11
protobuf>=4.25.3,<5
PySide6>=6.8,<7
pytest>=8,<9

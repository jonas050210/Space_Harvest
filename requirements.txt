# Space Harvest — orbital farming on real launch windows.
# Verified with Python 3.11-3.13 and Ursina 8.3.0 (Panda3D 1.10.16).
# Runtime deps:
ursina>=6.0.0
numpy>=1.26
pillow>=10.0

# Dev / optional (install with pip install -r requirements.txt or via [dev] extra):
# matplotlib>=3.8        # optional: porkchop plotting helper in tools/
# pyinstaller>=6.0.0     # optional: single-exe packaging, see README
# pytest>=8,<9           # dev: test suite
# ruff                   # dev: lint

# For full dev install: pip install -e ".[dev]"
